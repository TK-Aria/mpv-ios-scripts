#!/bin/sh
set -e

PREFIX="${COMMON_OPTIONS%% *}"
PREFIX="${PREFIX##*=}"

SHADERC_OPTIONS="-DCMAKE_INSTALL_PREFIX=$PREFIX -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=false -DCMAKE_OSX_SYSROOT=$SDKPATH"

cmake -GNinja $SHADERC_OPTIONS $SRC/shaderc*/
ninja